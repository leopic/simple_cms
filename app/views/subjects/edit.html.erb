
	<% @page_title = "subjects list" %>

	<%= link_to("< Back to list", {:action => "list"}, :class => "btn" ) %>
	<%= form_for(:subject, :url => {:action => "update", :id => @subject.id}) do |f| %>

	<%= render(:partial => "form", :locals => {:f => f}) %>
	
	<%= submit_tag("Update Subject") %>
	<% end %>