	
	<% @page_title = "subjects add" %>
	
	<h1>Add Subject</h1>
	
	<%= link_to("< Back to list", {:action => "list"}, :class => "back-link btn") %>
	
	<%= form_for(:subject, :url => {:action => "create"}) do |f| %>

		<%= render(:partial => "form", :locals => {:f => f}) %>
	
		<%= submit_tag("Create Subject", :class => "btn primary") %>
		
	<% end %>
