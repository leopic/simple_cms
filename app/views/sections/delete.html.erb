<!DOCTYPE html>
<html>
<head>
    <title>Sections - Delete</title>
</head>
<body>
<div id="wrapper">
    <h1>Delete Sections</h1>
	<%= link_to("< Back to list", {:action => "list"}, :class => "back-link") %>
	<%= form_for(:section, :url => {:action => "destroy", :id => @section.id}) do |f| %>
		<h2>se va a mandar el registro a lo macho?</h2>
		<p><%= @section.name %></p>
		<%= submit_tag("Delete Section") %>	
	<% end %>
</div>
</body>
</html>