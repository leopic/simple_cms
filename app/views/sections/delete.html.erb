	<% @page_title = "sections delete" %>

    <h1>Delete Sections</h1>
	
	<%= link_to("< Back to sections", {:action => "list", :page_id => @page.id, :subject_id => params[:subject_id]}, :class => "back-link btn") %>
	
	<%= form_for(:section, :url => {:action => "destroy", :id => @section.id}) do |f| %>
		<h2><%= @section.name %></h2>
		<p>se va a mandar el registro <strong><%= @section.name %></strong> a lo macho?</p>
		<div class="actions">
			<%= submit_tag("Delete", :class => "btn danger") %>	
            <%= link_to("Cancel", {:action => "list", :page_id => @page.id, :subject_id => params[:subject_id]}, :class => "btn") %>
		</div>
	<% end %>