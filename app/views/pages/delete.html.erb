	<% @page_title = "page delete" %>
	
    <h1>Delete Page</h1>
    
	<%= link_to("< Back to list", {:action => "list"}, :class => "back-link btn") %>
	
	<%= form_for(:page, :url => {:action => "destroy", :id => @page.id}) do |f| %>
		<h2><%= @page.name %></h2>
		<p>se va a mandar el registro <%= @page.name %> a lo macho?</p>
		<div class="actions">
			<%= submit_tag("Delete", :class => "btn danger") %>	
			<%= link_to("Cancel", {:action => "list"}, :class => "back-link btn") %>
		</div>
		
	<% end %>
